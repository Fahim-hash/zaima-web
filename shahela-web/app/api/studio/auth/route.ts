import { NextResponse } from 'next/server';
import { studioAdminSignature } from '@/lib/studio-auth';
export async function POST(request:Request){const body=await request.json();const password=String(body.password||'');if(!process.env.STUDIO_ADMIN_PASSWORD)return NextResponse.json({error:'Studio admin password is not configured.'},{status:500});if(password!==process.env.STUDIO_ADMIN_PASSWORD)return NextResponse.json({error:'Invalid password.'},{status:401});const response=NextResponse.json({ok:true});response.cookies.set('studio_admin',studioAdminSignature(),{httpOnly:true,secure:true,sameSite:'lax',path:'/',maxAge:60*60*24*7});return response;}
